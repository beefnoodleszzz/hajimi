# Master Handoff

A file in this directory is only accepted by the release workflow after
`hajimi qc master EP001_earth-stop` returns `PASS`. The local rebuild produces
an animatic-derived release candidate for pipeline verification; the intended
final picture, Fusion graphics, and Fairlight mix remain Resolve-owned.
